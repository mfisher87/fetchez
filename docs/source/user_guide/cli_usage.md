# 💻 Command Line Interface

The `fetchez` command line tool allows you to chain modules and hooks together.

```{eval-rst}
.. sphinx_argparse_cli::
   :module: fetchez.cli
   :func: get_parser
   :prog: fetchez
```